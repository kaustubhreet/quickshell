//import './App.css';
import React from 'react';
import Main from './component/Main';


function App() {

  return (
    <>
    
    <Main/>
{/*
<Dummy/>
    <div>
      <h1>create react app</h1>
      <div>
        <h2>Users contents</h2>
        {users.length > 0 && (
          <ul>
            {users.map(user => (
              <li key={user.id}>
                <p>
                  {user.id}
                </p>
                <p>
                  {user.name}
                </p>
                <p>
                  {user.available + 1}
                </p></li>

            ))}
          </ul>
        )}
      </div>

      <div>
        <h2>Tickets contents</h2>
        {tickets.length > 0 && (
          <ul>
            {tickets.map(ticket => (
              <li key={ticket.id}>
                <div>
                  <h1>{ticket.title}</h1>
                  <h3>{ticket.id}</h3>
                  <p>UserId: {ticket.userId} Priority: {ticket.priority} status: {ticket.status}</p>
                  <p>Tags= {ticket.tag}</p>
                </div>
              </li>
            ))}
          </ul>
        )}
      </div>

      <div>
  {tickets.map((ticket) => (
    <Box key={ticket.id} ticket={ticket} />
  ))}
</div>

    </div>
  */}
    </>
  );
}

export default App;
